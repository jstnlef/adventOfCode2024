module Day18



module Tests =
  open Xunit

  [<Theory>]
  [<InlineData("Inputs/Day18/test.txt", -1)>]
  [<InlineData("Inputs/Day18/input.txt", -1)>]
  let ``Part 1: Minimum steps to reach the exit`` (filename: string, expected: int) =
    let result = 0
    Assert.Equal(expected, result)

  [<Theory>]
  [<InlineData("Inputs/Day18/test.txt", -1)>]
  [<InlineData("Inputs/Day18/input.txt", -1)>]
  let ``Part 2`` (filename: string, expected: int) =
    let result = 0
    Assert.Equal(expected, result)
